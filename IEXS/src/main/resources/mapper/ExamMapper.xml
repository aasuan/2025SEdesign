<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.ExamMapper">

    <resultMap id="BaseResultMap" type="Exam">
        <id property="id" column="id"/>
        <result property="examName" column="exam_name"/>
        <result property="description" column="description"/>
        <result property="duration" column="duration"/>
        <result property="totalScore" column="total_score"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="creatorId" column="creator_id"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectById" parameterType="Integer" resultMap="BaseResultMap">
        SELECT * FROM exam WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM exam ORDER BY create_time DESC
    </select>

    <select id="selectByCreatorId" parameterType="Integer" resultMap="BaseResultMap">
        SELECT * FROM exam WHERE creator_id = #{creatorId} ORDER BY create_time DESC
    </select>

    <select id="selectAvailableExams" resultMap="BaseResultMap">
        SELECT * FROM exam 
        WHERE status IN (0, 1) 
        AND end_time > NOW()
        ORDER BY start_time ASC
    </select>

    <insert id="insert" parameterType="Exam" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO exam (exam_name, description, duration, total_score, start_time, end_time, creator_id, status)
        VALUES (#{examName}, #{description}, #{duration}, #{totalScore}, #{startTime}, #{endTime}, #{creatorId}, #{status})
    </insert>

    <update id="update" parameterType="Exam">
        UPDATE exam
        <set>
            <if test="examName != null">exam_name = #{examName},</if>
            <if test="description != null">description = #{description},</if>
            <if test="duration != null">duration = #{duration},</if>
            <if test="totalScore != null">total_score = #{totalScore},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="Integer">
        DELETE FROM exam WHERE id = #{id}
    </delete>

</mapper>

